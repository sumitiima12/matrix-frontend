{
  "name": "matrix",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "smoke": "node scripts/smoke.mjs",
    "lint": "eslint src",
    "test:unit": "node --import ./test/register.mjs --test test/*.test.mjs",
    "check": "eslint src && node --import ./test/register.mjs --test test/*.test.mjs && node scripts/smoke.mjs",
    "predeploy": "eslint src && node --import ./test/register.mjs --test test/*.test.mjs && node scripts/smoke.mjs && vite build",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:ci": "playwright install chromium && vite build && playwright test",
    "e2e:setup": "npm ci && npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.12.7",
    "lucide-react": "^0.383.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@vitejs/plugin-react": "^4.3.1",
    "esbuild": "^0.23.0",
    "eslint": "^9.0.0",
    "eslint-plugin-react-hooks": "^5.0.0",
    "vite": "^5.4.0"
  }
}
